@model IEnumerable<Product>;
@if (Model.Count()== 0)
{
    <h1>Список сравнения пуст</h1>
}
else
/*Хотел сделать на примере спортмастера , но пока не понимаю как слева сделать наименования параметров (пример Название товара : Кроссовки  Ботинки 
                                                                                                               Цена:             700        500             */           
{ <div class="row">
        @foreach (var p in Model)
         {
            <div class="col-3 ">
                <p><img width="200" height="100" src="@p.ImagePath" /></p>
                <p> @p.Name </p>
                <p> @p.Cost </p>
            </div>
         }
  </div>
  <p> <a class="btn btn-danger" role="button" href="/Comparison/Clear">Очистить список</a> </p>
}
